<div class="avalara-tax-code-select">
    <sw-single-select
        :value="localValue"
        :label="label || $tc('avalara-tax-code-select.title')"
        :disabled="disabled || isLoading"
        :options="taxCodeOptions"
        :is-loading="isLoading"
        :placeholder="$tc('avalara-tax-code-select.placeholder')"
        @update:value="onSelect"
    />
    <sw-button
        size="small"
        :disabled="isLoading"
        :is-loading="isLoading"
        @click="onRefresh"
    >
        {{ $tc('avalara-tax-code-select.refresh') }}
    </sw-button>
</div>
